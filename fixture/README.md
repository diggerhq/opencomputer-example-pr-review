# fixture: orders api

Minimal Express orders service the example agent reviews. The repository's
seeded pull requests change this app; they contain planted, findable bugs.

```bash
cd fixture
npm install
npm run dev
```
